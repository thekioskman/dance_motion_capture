import requests

"""
Note: Should run this script after register_users.py

This script can be used to populate the database with new clubs, add events for clubs,
add memberships for clubs, and add event interests for clubs
"""
# The URL of your FastAPI server's register endpoint
BASE_URL = "http://localhost:8000" 